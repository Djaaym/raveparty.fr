import AccountView from "@/components/AccountView";

export const metadata = { title: "My account — Favourites & alerts | RaveRadar" };

export default function Page() {
  return <AccountView lang="en" />;
}
